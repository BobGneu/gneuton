image:
- Visual Studio 2015
- Visual Studio 2017

version: '1.0.0.{build}'
platform: 
  - Any CPU

configuration:
  - Debug
  - Release

before_build:
- nuget restore Gneuton.sln

build:
  project: Gneuton.sln
  verbosity: minimal

test:
  assemblies:
    - 'Build\Output\*Tests.dll'

deploy: off